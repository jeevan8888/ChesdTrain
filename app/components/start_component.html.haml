.container.mx-auto.px-4.py-8
  .max-w-md.mx-auto.bg-white.rounded-xl.shadow-md.overflow-hidden
    .p-8
      %h2.text-xl.font-semibold.text-center.mb-6 Let's Start With The Basics
      
      = form_with(url: '#', class: 'space-y-4') do |f|
        .form-group
          = f.label :potluck_name, "What should we call this potluck?", class: 'block text-sm font-medium text-gray-700 mb-1'
          = f.text_field :potluck_name, class: 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'
        
        .form-group
          = f.label :date, "What date is this potluck called for?", class: 'block text-sm font-medium text-gray-700 mb-1'
          = f.text_field :date, class: 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'
        
        .form-group
          = f.label :time, "What time is the potluck?", class: 'block text-sm font-medium text-gray-700 mb-1'
          = f.text_field :time, class: 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500'
        
        .mt-6.flex.justify-center
          = f.submit "Next", class: 'w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-200'
        
        .text-center.text-sm.text-gray-500.mt-4
          Step #{@current_step} of #{@total_steps} 